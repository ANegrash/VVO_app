<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Vvo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkBack</item>
        <item name="colorPrimaryVariant">@color/darkBack</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.dropdown" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="android:spinnerDropDownItemStyle">@style/mySpinnerItemStyle</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="mySpinnerItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>